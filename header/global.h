#ifndef GLOBAL_H
#define GLOBAL_H
#include<string>
#include <algorithm>
#include <sstream>

using namespace std;
string toLowerCase(const string& str);
string formatCurrency(long long number);

#endif